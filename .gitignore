# Node modules
node_modules/
frontend/node_modules/
backend/node_modules/

# Python cache
__pycache__/
*.pyc

# Virtual environments / test tooling
venv/
.venv/
.venv-pytest/
.pytest_cache/

# Environment files with secrets (allow committed examples only)
.env
.env.*
!.env.example
!**/.env.example

# Docker artifacts
*.pid
*.log

# Certificates, keys, and secrets
pem/
*.pem
*.key
credentials.json
.aws/

# MacOS / system
.DS_Store

# Local builds and deploy artifacts
frontend/build/
*.tar.gz

# Terraform local cache and state (never commit state)
infra/.terraform/
infra/*.tfstate
infra/*.tfstate.*
infra/crash.log
**/*.tfvars
!infra/*.tfvars.example

# Legacy nginx logs (if recreated at repo root only)
/nginx/

# Runtime session / category data
flask_session/
sessions/
backend/sessions/
backend/categories.csv
backend/data.csv
backend/test_emails/
